# Examples
Input:
Added files: []
Deleted files: []
Modified files: ['references.bib']
Current .gitignore Content: (empty)

Since only a source/config file (.bib) was modified and there are no build artifacts, dependencies, or temp files in the changes, no ignores are needed.

Output: (empty — output nothing)